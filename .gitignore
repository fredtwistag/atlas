# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
.vscode

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local
.env

# typescript
*.tsbuildinfo

# claude code local tooling (worktrees, plugin cache, machine-specific state)
.claude/

# this is an npm project — no yarn lockfile
yarn.lock

# embedded-postgres test data
.pgdata-test

# playwright e2e artifacts
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Verify-build output dir (NEXT_DIST_DIR=.next-verify) — never commit build artifacts
/.next-verify/
